package handlers

/*
import (
	"context"
	"github.com/a-h/templ"
	"net/http"
)

type Pins struct {
	userService UserService
	username    string
	ctx         context.Context
}

func NewPins(ctx context.Context, u UserService, username string) *Pins {
	p := new(Pins)
	p.ctx = ctx
	p.userService = u
	p.username = username
	return p
}

func (p *Pins) GetComponent() *templ.Component {
	component := components.Pins()
}

func (p *Pins) ServeHTTP(w http.ResponseWriter, r *http.Request) {

}
*/
